/**
 * 海报 feature 类型重导出
 * 海报数据模型定义在 character/types.ts（共享同一域）
 */

export type {
  PosterType,
  PosterField,
  PosterTemplate,
  PosterArtSpec,
  PosterLayer,
  PosterDraft,
} from '@/features/character/types'
